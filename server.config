[params]
;server ip. this should be the machine IP. if it is set to localhost, then the server will respond only to the requests from localhost.
ip = 127.0.0.1
;server port. make sure that it's whitelisted in the firewall
port = 80
